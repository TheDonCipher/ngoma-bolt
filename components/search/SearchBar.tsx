import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <input type="text" placeholder="Search..." />
      {/* Implement search bar component here */}
    </div>
  );
};

export default SearchBar;