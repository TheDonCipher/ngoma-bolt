import React from 'react';

const ExploreLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      <h1>Explore Layout</h1>
      {children}
    </div>
  );
};

export default ExploreLayout;